public class Solution {
    public int numTrees(int n) {
        int[] c = new int[n+1];
        c[0]=1;
        for(int i=1;i<=n;i++)
            for(int j=0;j<i;j++)
                c[i]+=c[j]*c[i-j-1];
        return c[n];
    }
}
